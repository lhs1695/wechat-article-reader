services:
  app:
    environment:
      WECHAT_ARTICLE_READER_SECRETS_FILE: /run/secrets/app-credentials
    secrets:
      - app-credentials

secrets:
  app-credentials:
    file: ${WECHAT_ARTICLE_READER_SECRETS_FILE_HOST:-.secrets/app.env}
